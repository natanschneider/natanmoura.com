export function Layout({ children }){
    return(
        <main
            className="bg-slate-50 dark:bg-slate-900 text-slate-900 dark:text-slate-300 min-h-screen antialiased"
        >{ children }</main>
    )
}
